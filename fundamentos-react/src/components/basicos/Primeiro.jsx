import React from "react"
import { ReactDOM } from "react-dom"
export default function primeiro() {
    const msg = "Seja bem Vindo Ao meu componente"
    return (
        <div>
            <h2>Primeiro componente</h2>
            <p>{msg}</p>
        </div>
    )
}

